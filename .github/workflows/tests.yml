name: Tests

on:
  workflow_dispatch:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        test-case: [
          "test_is_7zip_installed",
          "test_alt_chars_candidates_generator",
          "test_7z_archives_extracted_via_fiddlesticks_CLI",
          "test_piping_candidates_to_stdout",
          "test_aegi_checker_from_CLI",
          "test_aegi_checker_errors_from_CLI",
          "test_aegis_checker_against_avdu_vault",
        ]
    steps:
    - name: Check out repo
      uses: actions/checkout@v7
    
    - name: Set up Python
      uses: actions/setup-python@v7
      with:
        python-version: "3.14"
    
    - name: Install dependencies
      run: |
        pip install -e .[tests,py7zr,aegis]

    - name: Run PyTest
      run: |
        pytest -k ${{ matrix.test-case }}